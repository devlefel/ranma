# Changelog

## [Unreleased]

### Added
- `ranma exec`, `add`, `rm`, `ls`, `link`, `whoami`, `shim`, `hook`, `doctor`
- Built-in providers: railway, gh, resend
- Credential import from `~/.railway/config.json` and `~/.config/gh/hosts.yml`
